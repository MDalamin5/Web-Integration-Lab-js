const accountId = 18811904
let accountEmail = "hello@byv.com"
var accountPassword = "testPass"
accountCity = "DHK"

console.table([accountId, accountEmail, accountPassword, accountCity])

/*
If you want to declear a variable as its never change its will be the const
and use let if its a dynamic variable. but in var has scoping problem so we did not use it.
*/
